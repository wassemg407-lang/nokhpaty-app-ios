workflows:
  ios-workflow:
    name: iOS Build

    environment:
      node: 18

    scripts:
      - npm install

      # نشغّل Capacitor مباشرة بدون npx
      - node ./node_modules/@capacitor/cli/bin/cap.js sync ios

    artifacts:
      - ios/App/build/ios/ipa/*.ipa