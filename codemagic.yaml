workflows:
  ios-workflow:
    name: iOS Build
    environment:
      node: latest

    scripts:
      - npm install
      - npx cap sync ios
      - npx cap copy ios

    artifacts:
      - ios/App/build/ios/ipa/*.ipa