workflows:
  ios-workflow:
    name: iOS Build

    environment:
      node: latest

    scripts:
      - npm ci
      - npm install @capacitor/core @capacitor/cli
      - npx cap add ios
      - npx cap sync ios

    artifacts:
      - ios/App/build/ios/ipa/*.ipa