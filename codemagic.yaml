workflows:
  ios-workflow:
    name: iOS Build

    environment:
      node: 18

    scripts:
      - npm install
      - npm install @capacitor/cli @capacitor/core
      - npx cap sync ios

    artifacts:
      - ios/App/build/ios/ipa/*.ipa