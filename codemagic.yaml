workflows:
  ios-workflow:
    name: iOS Build

    environment:
      node: latest

    scripts:
      - npm install
      - npm install @capacitor/cli @capacitor/core
      - npx cap sync ios || node node_modules/@capacitor/cli/bin/cap.js sync ios

    artifacts:
      - ios/App/build/ios/ipa/*.ipa