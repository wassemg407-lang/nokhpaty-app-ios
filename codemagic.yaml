workflows:
  ios-workflow:
    name: iOS Build

    environment:
      node: latest

    scripts:
      - npm ci
      - npm install @capacitor/core @capacitor/cli
      - npx cap sync ios
      - cd ios/App && pod install
      - cd ../..

    artifacts:
      - ios/App/build/ios/ipa/*.ipa